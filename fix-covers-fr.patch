diff --git a/.gitignore b/.gitignore
new file mode 100644
index 0000000..4de24e6
--- /dev/null
+++ b/.gitignore
@@ -0,0 +1,7 @@
+__pycache__/
+*.pyc
+cache.db
+cache.db-shm
+cache.db-wal
+static/img_cache/
+.venv/
diff --git a/routes/home.py b/routes/home.py
index ce26938..b84a997 100644
--- a/routes/home.py
+++ b/routes/home.py
@@ -77,8 +77,8 @@ async def index(
         error = "😴 Noka n'arrive pas à joindre la bibliothèque… Réessaie dans un instant !"
 
     has_next_page = list_page < total_pages
-    previous_page_url = f"/?list={list_page - 1}" if list_page > 1 else None
-    next_page_url = f"/?list={list_page + 1}" if has_next_page else None
+    previous_page_url = f"/fr/?list={list_page - 1}" if list_page > 1 else None
+    next_page_url = f"/fr/?list={list_page + 1}" if has_next_page else None
 
     return templates.TemplateResponse(
         request,
@@ -138,7 +138,7 @@ def sitemap() -> Response:
     
     # 1. URL de la page d'accueil (version française sous /fr)
     xml_lines.append("    <url>")
-    xml_lines.append(f"        <loc>{base_url}/</loc>")
+    xml_lines.append(f"        <loc>{base_url}/fr</loc>")
     xml_lines.append(f"        <lastmod>{today}</lastmod>")
     xml_lines.append("        <changefreq>daily</changefreq>")
     xml_lines.append("        <priority>1.0</priority>")
@@ -154,7 +154,7 @@ def sitemap() -> Response:
         else:
             lastmod = today
         xml_lines.append("    <url>")
-        xml_lines.append(f"        <loc>{base_url}/manga/{slug}</loc>")
+        xml_lines.append(f"        <loc>{base_url}/fr/manga/{slug}</loc>")
         xml_lines.append(f"        <lastmod>{lastmod}</lastmod>")
         xml_lines.append("        <changefreq>weekly</changefreq>")
         xml_lines.append("        <priority>0.8</priority>")
@@ -194,7 +194,7 @@ def sitemap_chapters() -> Response:
             ch_num = chapter.get("number")
             if ch_num:
                 chapters_with_time.append({
-                    "url": f"{base_url}/read/{slug}/{ch_num}",
+                    "url": f"{base_url}/fr/read/{slug}/{ch_num}",
                     "time": creation_time,
                 })
     
@@ -271,7 +271,7 @@ async def rss_feed() -> Response:
                 filename, _ = get_cache_filename(cover)
                 # Forcer l'extension en .jpg pour assurer la compatibilité universelle sur les réseaux (Pinterest, n8n, etc.)
                 filename_jpg = filename.rsplit(".", 1)[0] + ".jpg"
-                cover_url = f"{base_url}/img-cdn/{filename_jpg}"
+                cover_url = f"{base_url}/fr/img-cdn/{filename_jpg}"
             except Exception as exc:
                 logger.warning("Failed to generate proxy cover URL for RSS: %s", exc)
                 cover_url = cover
@@ -287,9 +287,9 @@ async def rss_feed() -> Response:
             pub_date_rfc = now_rfc822
 
         # Le GUID est désormais unique par chapitre pour forcer la publication automatique de Pinterest
-        unique_guid = f"{base_url}/manga/{slug}#ch-{latest_ch_num}"
+        unique_guid = f"{base_url}/fr/manga/{slug}#ch-{latest_ch_num}"
         # Le lien de l'article pointe directement vers le lecteur de chapitre si disponible pour maximiser le taux de conversion
-        chapter_link = f"{base_url}/read/{slug}/{latest_ch_num}" if chapters else f"{base_url}/manga/{slug}"
+        chapter_link = f"{base_url}/fr/read/{slug}/{latest_ch_num}" if chapters else f"{base_url}/fr/manga/{slug}"
 
         rss_items.append(f"""        <item>
             <title>Lire {title} Chapitre {latest_ch_num} en ligne gratuit </title>
@@ -304,7 +304,7 @@ async def rss_feed() -> Response:
 <rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
     <channel>
         <title>MangaNoka - Dernières mises à jour manga</title>
-        <link>{base_url}/</link>
+        <link>{base_url}/fr</link>
         <description>Lecture de manga rapide, responsive et sans publicité.</description>
         <language>fr-fr</language>
         <lastBuildDate>{now_rfc822}</lastBuildDate>
diff --git a/routes/manga.py b/routes/manga.py
index ca68031..09b54ab 100644
--- a/routes/manga.py
+++ b/routes/manga.py
@@ -68,8 +68,8 @@ async def manga_detail(request: Request, slug: str, background_tasks: Background
 
     # Indexation + Webhook : Ne déclencher que pour les vrais utilisateurs (pas les bots)
     if not _is_bot_request(request):
-        background_tasks.add_task(ping_indexnow, [f"/manga/{slug}"])
-        background_tasks.add_task(ping_google_indexing, [f"/manga/{slug}"])
+        background_tasks.add_task(ping_indexnow, [f"/fr/manga/{slug}"])
+        background_tasks.add_task(ping_google_indexing, [f"/fr/manga/{slug}"])
         # Webhook Make.com : déclenché ici dans la route, pas dans le loader de cache
         background_tasks.add_task(_trigger_webhook, manga, slug)
     else:
diff --git a/routes/reader.py b/routes/reader.py
index 2af0484..2ebe4b4 100644
--- a/routes/reader.py
+++ b/routes/reader.py
@@ -98,8 +98,8 @@ async def read_chapter(request: Request, slug: str, chapter: str, background_tas
 
     # Indexation + Webhook : Ne déclencher que pour les vrais utilisateurs (pas les bots)
     if not _is_bot_request(request):
-        background_tasks.add_task(ping_indexnow, [f"/read/{slug}/{chapter}"])
-        background_tasks.add_task(ping_google_indexing, [f"/read/{slug}/{chapter}"])
+        background_tasks.add_task(ping_indexnow, [f"/fr/read/{slug}/{chapter}"])
+        background_tasks.add_task(ping_google_indexing, [f"/fr/read/{slug}/{chapter}"])
         # Webhook Make.com : déclenché ici dans la route, pas dans le loader de cache
         # Ainsi il s'exécute à chaque visite, protégé par l'anti-doublon GUID
         background_tasks.add_task(
diff --git a/routes/search.py b/routes/search.py
index ebeda50..35fe561 100644
--- a/routes/search.py
+++ b/routes/search.py
@@ -123,8 +123,8 @@ async def search(
             error = "Impossible d'effectuer la recherche pour le moment."
 
     has_next_page = len(mangas) >= 20
-    previous_page_url = f"/search?q={q}&p={page - 1}" if page > 1 else None
-    next_page_url = f"/search?q={q}&p={page + 1}" if has_next_page else None
+    previous_page_url = f"/fr/search?q={q}&p={page - 1}" if page > 1 else None
+    next_page_url = f"/fr/search?q={q}&p={page + 1}" if has_next_page else None
 
     return templates.TemplateResponse(
         request,
diff --git a/services/phenix_scans.py b/services/phenix_scans.py
index 83cdc3a..bbb8358 100644
--- a/services/phenix_scans.py
+++ b/services/phenix_scans.py
@@ -161,7 +161,7 @@ class PhenixScansAPI:
                                 {
                                     "number": ch_num,
                                     "title": f"Chapitre {ch_num}",
-                                    "url": f"/read/{slug}/{ch_num}",
+                                    "url": f"/fr/read/{slug}/{ch_num}",
                                     "date": self._format_date(
                                         ch.get("createdAt", "")
                                     ),
@@ -279,7 +279,7 @@ class PhenixScansAPI:
                             {
                                 "number": ch_num,
                                 "title": f"Chapitre {ch_num}",
-                                "url": f"/read/{slug}/{ch_num}",
+                                "url": f"/fr/read/{slug}/{ch_num}",
                                 "date": self._format_date(ch.get("createdAt", "")),
                             }
                         )
diff --git a/services/webhook_pusher.py b/services/webhook_pusher.py
index 9bd0089..815c60d 100644
--- a/services/webhook_pusher.py
+++ b/services/webhook_pusher.py
@@ -159,7 +159,7 @@ async def push_to_make_webhook(manga_title: str, slug: str, latest_ch_num: str,
             return
     
     base_url = os.environ.get("BASE_URL", "https://www.manganoka.xyz").rstrip("/")
-    guid = f"{base_url}/manga/{slug}#ch-{latest_ch_num}"
+    guid = f"{base_url}/fr/manga/{slug}#ch-{latest_ch_num}"
     
     # 1. Éviter 100 % des doublons : si le chapitre est déjà publié, on s'arrête immédiatement !
     if is_guid_posted(guid):
@@ -210,7 +210,7 @@ async def push_to_make_webhook(manga_title: str, slug: str, latest_ch_num: str,
                     service.upload_to_s3(service.get_s3_object_key(filename), image_data, content_type)
             
             # URL finale avec extension .jpg → conversion JPEG automatique dans /img-cdn
-            image_url = f"{base_url}/img-cdn/{filename_jpg}"
+            image_url = f"{base_url}/fr/img-cdn/{filename_jpg}"
         except Exception as exc:
             logger.warning("Webhook Pusher: Échec préchargement image, utilisation og_image: %s", exc)
             image_url = f"{base_url}/static/og_image.png"
@@ -243,7 +243,7 @@ async def push_to_make_webhook(manga_title: str, slug: str, latest_ch_num: str,
 
     payload = {
         "title": pin_title,
-        "link": f"{base_url}/read/{slug}/{latest_ch_num}",
+        "link": f"{base_url}/fr/read/{slug}/{latest_ch_num}",
         "description": seo_desc,
         "image_url": image_url,
         "guid": guid
diff --git a/static/sw.js b/static/sw.js
index aace5d6..18e2282 100644
--- a/static/sw.js
+++ b/static/sw.js
@@ -43,9 +43,9 @@ self.addEventListener('fetch', (e) => {
 
     // Ne pas intercepter : API, proxy d'images, routes d'administration
     if (
-        url.pathname.startsWith('/api/') ||
-        url.pathname.startsWith('/img-proxy') ||
-        url.pathname.startsWith('/img-cdn') ||
+        url.pathname.startsWith('/fr/api/') ||
+        url.pathname.startsWith('/fr/img-proxy') ||
+        url.pathname.startsWith('/fr/img-cdn') ||
         url.pathname.startsWith('/admin')
     ) {
         return;
diff --git a/templates/history.html b/templates/history.html
index a5b541d..0bf17cb 100644
--- a/templates/history.html
+++ b/templates/history.html
@@ -72,7 +72,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
@@ -169,7 +169,7 @@
         if (!results.length) { dropdown.hidden = true; return; }
         dropdown.innerHTML = results.map((r, i) => `
             <a class="sd-item" href="/fr/manga/${r.slug}" data-index="${i}">
-                <img class="sd-thumb" src="/img-proxy?url=${r.cover}" alt="${r.title}" loading="lazy">
+                <img class="sd-thumb" src="/fr/img-proxy?url=${r.cover}" alt="${r.title}" loading="lazy">
                 <div class="sd-info">
                     <span class="sd-title">${r.title}</span>
                     ${r.views ? `<span class="sd-views">👁 ${Number(r.views.replace(/\s/g,'')).toLocaleString('en-US')}</span>` : ''}
@@ -181,7 +181,7 @@
     }
 
     function fetchResults(q) {
-        fetch(`/api/fr/search?q=${encodeURIComponent(q)}`)
+        fetch(`/fr/api/search?q=${encodeURIComponent(q)}`)
             .then(r => r.json())
             .then(data => renderDropdown(data.results || []))
             .catch(() => { dropdown.hidden = true; });
@@ -290,7 +290,7 @@ document.addEventListener("DOMContentLoaded", () => {
         bookmarksList.innerHTML = bookmarks.map(item => `
             <div class="user-bookmark-item" style="display: flex; gap: 15px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 15px; align-items: flex-start; text-align: left;">
                 <a href="/fr/manga/${item.slug}">
-                    <img src="/img-proxy?url=${item.cover}" alt="${item.title}" style="width: 65px; height: 85px; object-fit: cover; border-radius: 4px;">
+                    <img src="/fr/img-proxy?url=${item.cover}" alt="${item.title}" style="width: 65px; height: 85px; object-fit: cover; border-radius: 4px;">
                 </a>
                 <div class="user-bookmark-item-right" style="display: flex; flex-direction: column; gap: 6px; flex-grow: 1;">
                     <span class="bm-title" style="font-size: 15px; font-weight: bold;">
@@ -338,7 +338,7 @@ document.addEventListener("DOMContentLoaded", () => {
         historyList.innerHTML = history.map(item => `
             <div class="user-history-item" style="display: flex; gap: 15px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 15px; align-items: flex-start; text-align: left;">
                 <a href="/fr/manga/${item.slug}">
-                    <img src="/img-proxy?url=${item.cover}" alt="${item.title}" style="width: 65px; height: 85px; object-fit: cover; border-radius: 4px;">
+                    <img src="/fr/img-proxy?url=${item.cover}" alt="${item.title}" style="width: 65px; height: 85px; object-fit: cover; border-radius: 4px;">
                 </a>
                 <div class="user-history-item-right" style="display: flex; flex-direction: column; gap: 6px; flex-grow: 1;">
                     <span class="bm-title" style="font-size: 15px; font-weight: bold;">
diff --git a/templates/index.html b/templates/index.html
index a741778..ac37f7b 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -155,7 +155,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
@@ -220,7 +220,7 @@
                     {% for item in popular %}
                     <div class="pop-item" style="--bg:url('/img-proxy?url={{ item.cover }}')" role="listitem">
                         <a href="/fr/manga/{{ item.slug }}" aria-label="Read {{ item.title }}{% if item.chapter.title %}, latest: {{ item.chapter.title }}{% endif %}">
-                            <img src="/img-proxy?url={{ item.cover }}" alt="{{ item.title }} cover" {% if loop.index <= 4 %}fetchpriority="high" loading="eager"{% else %}loading="lazy"{% endif %} decoding="async" width="160" height="220">
+                            <img src="/fr/img-proxy?url={{ item.cover }}" alt="{{ item.title }} cover" {% if loop.index <= 4 %}fetchpriority="high" loading="eager"{% else %}loading="lazy"{% endif %} decoding="async" width="160" height="220">
                         </a>
                         <div class="pop-overlay">
                             <h3 class="pop-title">
@@ -276,7 +276,7 @@
                     <article class="release-item">
                         <a href="/fr/manga/{{ manga.slug }}" class="release-cover" aria-label="View {{ manga.title }}">
                             {% if manga.cover %}
-                            <img src="/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} cover" loading="lazy" decoding="async" width="70" height="95" style="display: block; overflow: hidden; white-space: nowrap; text-overflow: ellipsis;">
+                            <img src="/fr/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} cover" loading="lazy" decoding="async" width="70" height="95" style="display: block; overflow: hidden; white-space: nowrap; text-overflow: ellipsis;">
                             {% else %}
                             <span aria-label="{{ manga.title }} cover placeholder">{{ manga.title[:1] }}</span>
                             {% endif %}
@@ -403,7 +403,7 @@
                 str += results.map((r, i) => `
             <a href="/fr/manga/${r.slug}">
                 <li id="${i}" class="option ${i === activeIndex ? 'selected' : ''}" style="display: flex; gap: 10px; align-items: center; border-bottom: 1px solid rgba(255,255,255,0.05); padding: 8px 12px; text-align: left;">
-                    <img class="search_result_img" src="/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
+                    <img class="search_result_img" src="/fr/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
                     <div class="search_result_item_right" style="display: flex; flex-direction: column; overflow: hidden; flex-grow: 1;">
                         <p class="search_result_row1" id="${r.slug}" style="font-size: 13.5px; font-weight: bold; margin: 0 0 4px 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--text-color);">${r.title}</p>
                         <p class="search_result_row2" style="font-size: 11px; margin: 0 0 2px 0; color: #888; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">${r.author || ''}</p>
@@ -427,7 +427,7 @@
             }
 
             function fetchResults(q) {
-                fetch(`/api/fr/search?q=${encodeURIComponent(q)}`)
+                fetch(`/fr/api/search?q=${encodeURIComponent(q)}`)
                     .then(r => r.json())
                     .then(data => renderDropdown(data.results || []))
                     .catch(() => { dropdown.hidden = true; });
@@ -502,7 +502,7 @@
                 const itemsMarkup = history.slice(0, 5).map(item => `
             <div class="history-sidebar-content-item" style="display: flex; align-items: center; gap: 10px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 8px; margin-top: 8px;">
                 <a rel="nofollow" href="/fr/manga/${item.slug}" class="comic-link" aria-label="${item.title}">
-                    <img src="/img-proxy?url=${item.cover}" alt="${item.title}" class="lazy" width="36" height="48" style="border-radius: 4px; object-fit: cover;">
+                    <img src="/fr/img-proxy?url=${item.cover}" alt="${item.title}" class="lazy" width="36" height="48" style="border-radius: 4px; object-fit: cover;">
                 </a>
                 <div style="display: flex; flex-direction: column; overflow: hidden; text-align: left; flex-grow: 1;">
                     <span class="history-sidebar-content-text comic-name" style="font-size: 13px; font-weight: bold; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; display: block; width: 100%;">
@@ -529,7 +529,7 @@
                 const itemsMarkup = bookmarks.slice(0, 5).map(item => `
             <div class="bookmarks-sidebar-content-item" style="display: flex; align-items: center; gap: 10px; border-bottom: 1px solid rgba(255,255,255,0.05); padding-bottom: 8px; margin-top: 8px;">
                 <a rel="nofollow" href="/fr/manga/${item.slug}" class="comic-link" aria-label="${item.title}">
-                    <img src="/img-proxy?url=${item.cover}" alt="${item.title}" class="lazy" width="36" height="48" style="border-radius: 4px; object-fit: cover;">
+                    <img src="/fr/img-proxy?url=${item.cover}" alt="${item.title}" class="lazy" width="36" height="48" style="border-radius: 4px; object-fit: cover;">
                 </a>
                 <div style="display: flex; flex-direction: column; overflow: hidden; text-align: left; flex-grow: 1;">
                     <span class="history-sidebar-content-text comic-name" style="font-size: 13px; font-weight: bold; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; display: block; width: 100%;">
diff --git a/templates/manga.html b/templates/manga.html
index 1d07e3b..b6161cc 100644
--- a/templates/manga.html
+++ b/templates/manga.html
@@ -118,7 +118,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
@@ -151,9 +151,9 @@
                 <article class="manga-profile">
                     <div class="profile-cover" style="position: relative; width: 100%; height: 320px; overflow: hidden; background: linear-gradient(135deg, var(--surface) 0%, rgba(0,0,0,0.3) 100%); border-radius: var(--radius-md) var(--radius-md) 0 0;">
                         {% if manga.cover %}
-                            <img src="/img-proxy?url={{ manga.cover }}" alt="Couverture de {{ manga.title }} - MangaNoka" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; object-position: center; opacity: 0.3; filter: blur(8px); transform: scale(1.1);" loading="eager" decoding="async">
+                            <img src="/fr/img-proxy?url={{ manga.cover }}" alt="Couverture de {{ manga.title }} - MangaNoka" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; object-position: center; opacity: 0.3; filter: blur(8px); transform: scale(1.1);" loading="eager" decoding="async">
                             <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); z-index: 2;">
-                                <img src="/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} couverture - MangaNoka" width="180" height="260" style="width: 180px; height: 260px; object-fit: cover; border-radius: var(--radius-md); box-shadow: 0 8px 24px rgba(0,0,0,0.5); border: 3px solid var(--surface);" loading="eager" fetchpriority="high">
+                                <img src="/fr/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} couverture - MangaNoka" width="180" height="260" style="width: 180px; height: 260px; object-fit: cover; border-radius: var(--radius-md); box-shadow: 0 8px 24px rgba(0,0,0,0.5); border: 3px solid var(--surface);" loading="eager" fetchpriority="high">
                             </div>
                         {% else %}
                             <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); width: 180px; height: 260px; background: var(--brand); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; font-size: 80px; font-weight: bold; color: #fff; box-shadow: 0 8px 24px rgba(0,0,0,0.5);">
@@ -249,7 +249,7 @@
                         {% for related in related_mangas %}
                         <a href="/fr/manga/{{ related.slug }}" class="related-manga-card" role="listitem" aria-label="View {{ related.title }}" style="display: flex; flex-direction: column; text-decoration: none; border-radius: var(--radius-md); overflow: hidden; background: var(--surface); transition: transform 0.2s, box-shadow 0.2s;" onmouseover="this.style.transform='translateY(-4px)'; this.style.boxShadow='0 8px 16px rgba(0,0,0,0.3)';" onmouseout="this.style.transform='translateY(0)'; this.style.boxShadow='none';">
                             <div style="position: relative; width: 100%; padding-bottom: 140%; overflow: hidden; background: var(--line);">
-                                <img src="/img-proxy?url={{ related.cover }}" alt="{{ related.title }} cover" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;">
+                                <img src="/fr/img-proxy?url={{ related.cover }}" alt="{{ related.title }} cover" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover;">
                             </div>
                             <div style="padding: 8px; flex-grow: 1;">
                                 <h3 style="font-size: 13px; font-weight: 600; margin: 0 0 4px 0; overflow: hidden; text-overflow: ellipsis; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; color: var(--text-color); line-height: 1.3;">{{ related.title }}</h3>
@@ -300,7 +300,7 @@
         str += results.map((r, i) => `
             <a href="/fr/manga/${r.slug}">
                 <li id="${i}" class="option ${i === activeIndex ? 'selected' : ''}" style="display: flex; gap: 10px; align-items: center; border-bottom: 1px solid rgba(255,255,255,0.05); padding: 8px 12px; text-align: left;">
-                    <img class="search_result_img" src="/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
+                    <img class="search_result_img" src="/fr/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
                     <div class="search_result_item_right" style="display: flex; flex-direction: column; overflow: hidden; flex-grow: 1;">
                         <p class="search_result_row1" id="${r.slug}" style="font-size: 13.5px; font-weight: bold; margin: 0 0 4px 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--text-color);">${r.title}</p>
                         <p class="search_result_row2" style="font-size: 11px; margin: 0 0 2px 0; color: #888; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">${r.author || ''}</p>
@@ -324,7 +324,7 @@
     }
 
     function fetchResults(q) {
-        fetch(`/api/fr/search?q=${encodeURIComponent(q)}`)
+        fetch(`/fr/api/search?q=${encodeURIComponent(q)}`)
             .then(r => r.json())
             .then(data => renderDropdown(data.results || []))
             .catch(() => { dropdown.hidden = true; });
diff --git a/templates/privacy.html b/templates/privacy.html
index 3947f05..7c5016d 100644
--- a/templates/privacy.html
+++ b/templates/privacy.html
@@ -34,7 +34,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
diff --git a/templates/search.html b/templates/search.html
index 26e995a..9da36c4 100644
--- a/templates/search.html
+++ b/templates/search.html
@@ -62,7 +62,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" value="{{ query }}" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
@@ -127,7 +127,7 @@
                         <article class="search-result-item">
                             <a href="/fr/manga/{{ manga.slug }}" class="result-cover" aria-label="View {{ manga.title }}">
                                 {% if manga.cover %}
-                                    <img src="/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} cover" loading="lazy">
+                                    <img src="/fr/img-proxy?url={{ manga.cover }}" alt="{{ manga.title }} cover" loading="lazy">
                                 {% else %}
                                     <span aria-label="{{ manga.title }} placeholder">{{ manga.title[:1] }}</span>
                                 {% endif %}
@@ -197,7 +197,7 @@
         str += results.map((r, i) => `
             <a href="/fr/manga/${r.slug}">
                 <li id="${i}" class="option ${i === activeIndex ? 'selected' : ''}" style="display: flex; gap: 10px; align-items: center; border-bottom: 1px solid rgba(255,255,255,0.05); padding: 8px 12px; text-align: left;">
-                    <img class="search_result_img" src="/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
+                    <img class="search_result_img" src="/fr/img-proxy?url=${r.cover}" style="width: 45px; height: 60px; object-fit: cover; border-radius: 4px;" />
                     <div class="search_result_item_right" style="display: flex; flex-direction: column; overflow: hidden; flex-grow: 1;">
                         <p class="search_result_row1" id="${r.slug}" style="font-size: 13.5px; font-weight: bold; margin: 0 0 4px 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; color: var(--text-color);">${r.title}</p>
                         <p class="search_result_row2" style="font-size: 11px; margin: 0 0 2px 0; color: #888; overflow: hidden; text-overflow: ellipsis; white-space: nowrap;">${r.author || ''}</p>
@@ -221,7 +221,7 @@
     }
 
     function fetchResults(q) {
-        fetch(`/api/fr/search?q=${encodeURIComponent(q)}`)
+        fetch(`/fr/api/search?q=${encodeURIComponent(q)}`)
             .then(r => r.json())
             .then(data => renderDropdown(data.results || []))
             .catch(() => { dropdown.hidden = true; });
diff --git a/templates/terms.html b/templates/terms.html
index 282d728..9a71e20 100644
--- a/templates/terms.html
+++ b/templates/terms.html
@@ -34,7 +34,7 @@
                 <img class="brand-logo" src="{{ url_for('static', path='/noka_logo.svg') }}" alt="MangaNoka logo">
                 <span>Manga<b>Noka</b></span>
             </a>
-            <form class="search-bar" action="/search" method="get" id="search-form">
+            <form class="search-bar" action="/fr/search" method="get" id="search-form">
                 <input type="text" name="q" id="search-input" placeholder="Rechercher un manga..." autocomplete="off" required>
                 <button type="submit" aria-label="Rechercher">
                     <i class="fas fa-search" aria-hidden="true"></i>
